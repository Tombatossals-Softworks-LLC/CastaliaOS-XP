add_executable(castalia-usuarios src/main.cpp)
target_link_libraries(castalia-usuarios PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-usuarios PRIVATE -s)
endif()
