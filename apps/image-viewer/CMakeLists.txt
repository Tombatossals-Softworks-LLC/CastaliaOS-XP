add_executable(castalia-visor src/main.cpp)
target_link_libraries(castalia-visor PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-visor PRIVATE -s)
endif()
