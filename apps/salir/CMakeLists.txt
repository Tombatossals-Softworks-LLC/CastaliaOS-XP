add_executable(castalia-salir src/main.cpp)
target_link_libraries(castalia-salir PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-salir PRIVATE -s)
endif()
