add_executable(castalia-bienvenida src/main.cpp)
target_link_libraries(castalia-bienvenida PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-bienvenida PRIVATE -s)
endif()
