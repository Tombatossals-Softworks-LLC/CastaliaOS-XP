add_executable(castalia-notas src/main.cpp)
target_link_libraries(castalia-notas PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-notas PRIVATE -s)
endif()
