add_executable(castalia-ejecutar src/main.cpp)
target_link_libraries(castalia-ejecutar PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-ejecutar PRIVATE -s)
endif()
