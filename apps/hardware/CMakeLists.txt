add_executable(castalia-hardware src/main.cpp)
target_link_libraries(castalia-hardware PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-hardware PRIVATE -s)
endif()
