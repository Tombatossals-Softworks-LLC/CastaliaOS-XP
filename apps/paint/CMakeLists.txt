add_executable(castalia-pintura src/main.cpp)
target_link_libraries(castalia-pintura PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-pintura PRIVATE -s)
endif()
