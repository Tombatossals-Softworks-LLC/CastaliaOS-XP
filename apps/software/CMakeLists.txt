add_executable(castalia-software src/main.cpp)
target_link_libraries(castalia-software PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-software PRIVATE -s)
endif()
