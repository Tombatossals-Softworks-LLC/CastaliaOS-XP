add_executable(castalia-fechahora src/main.cpp)
target_link_libraries(castalia-fechahora PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-fechahora PRIVATE -s)
endif()
