add_executable(castalia-redes src/main.cpp)
target_link_libraries(castalia-redes PRIVATE castalia-ui)
if(CMAKE_BUILD_TYPE STREQUAL "Release")
    target_link_options(castalia-redes PRIVATE -s)
endif()
